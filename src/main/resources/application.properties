# ============================================
# CONFIGURACION TAVERNMASTER - AWS CON FLYWAY
# CON NOMBRES DE TABLAS EN MAYUSCULAS
# ============================================

# ----- CONEXION MYSQL EN AWS -----
spring.datasource.url=jdbc:mysql://3.210.180.171:3306/TavernMaster?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&connectTimeout=60000
spring.datasource.username=admin
spring.datasource.password=g4bTHzZSMePE
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ----- FLYWAY CONFIGURACION - GESTION DE MIGRACIONES -----
# ACTIVA FLYWAY PARA CONTROL DE VERSIONES DE BBDD
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0
spring.flyway.locations=classpath:db/migration
spring.flyway.table=flyway_schema_history
spring.flyway.validate-on-migrate=true

# ----- JPA / HIBERNATE -----
# validate = NO CREA NI MODIFICA TABLAS, SOLO VALIDA QUE EXISTEN
# LAS TABLAS LAS GESTIONA FLYWAY, NO HIBERNATE
spring.jpa.hibernate.ddl-auto=validate

# FORZAR NOMBRES DE TABLAS EN MAYUSCULAS (CAMPANA, JUGADOR, PERSONAJE)
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# ----- MOSTRAR SQL EN CONSOLA (OPCIONAL) -----
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ----- LOGGING (OPCIONAL) -----
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.flywaydb=INFO